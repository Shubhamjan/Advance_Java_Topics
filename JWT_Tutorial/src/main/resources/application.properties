spring.application.name=JWT_Tutorial
# =======================
# SERVER CONFIGURATION
# =======================
server.port=8084

# =======================
# DATABASE CONFIGURATION
# =======================
spring.datasource.url=jdbc:mysql://localhost:3306/jwt?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Shubham@242#
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# =======================
# JWT CONFIGURATION
# =======================
jwt_secret=MySuperSecretKeyForJWT12345
jwt_expiration=3600000   # 1 hour in milliseconds
